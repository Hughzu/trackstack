---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<div class:list={["flex flex-col gap-4 bg-surface rounded-2xl p-4 border border-border", className]}>
  <slot />
</div>
