---
import CoreLayout from "@/core/layouts/Layout.astro";
import { appConfig } from "@app/config";

interface Props {
  title?: string;
}

const { title = appConfig.appName } = Astro.props;
---

<CoreLayout title={title}>
  <slot />
</CoreLayout>
